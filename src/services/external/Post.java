package services.external;

public class Post {
    public Post(String username, String password) {
        System.out.println("Post instantiated with username: " + username + " and password: " + password);
    }
}
